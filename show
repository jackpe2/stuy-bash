echo -e "abc\tabc"

# 逆序 一个空隔 相隔的 字符串
for i in $string; do echo $i ;done | sort -r
#思路描述：
#  使用 for 循环遍历字符串，本身就是 以空格拆分，就像在python 中 把一个字符串使用 split("\s") 拆分成列表，然后用for 循环遍历一样
#  这样使用 for 把一个一行的字符串，就拆分成一行一个单词的字符串了，此时再 管道 给 sort -r 逆序一下，就得到了它的 逆序 字符串结果了

